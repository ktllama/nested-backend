{
    "name": "nested-backend",
    "version": "1.0.0",
    "description": "Nested roommate matching app backend, routes and server",
    "main": "server.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "node server",
        "dev": "nodemon server.js"
    },
    "dependencies": {
        "body-parser": "^1.19.2",
        "express": "^4.17.1",
        "morgan": "^1.10.0"
    },
    "devDependencies": {
        "nodemon": "^2.0.15"
    }
}
